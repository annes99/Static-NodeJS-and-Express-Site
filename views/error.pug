extends layout.pug

block content
  nav.grid-container.portfolio-breadcrumb
    a(href='/') ← Back

  h3.colorB oops, something went wrong

  p= `${error.message} - Error ${error.status}`

  .paper
  pre.stack-trace= error.stack
  .tear